# OpenOpportunity — moved

This repository is a placeholder that protects the `open-opportunity` name from
squatting (D8 / TASK-P-0002: two-repo split).

OpenOpportunity now lives in two repos:

| Repo | Visibility | Holds |
|---|---|---|
| [`itsmeront/open-opportunity-protocol`](https://github.com/itsmeront/open-opportunity-protocol) | public | Protocol + curated transparency (D8 public side) |
| [`itsmeront/open-opportunity-platform`](https://github.com/itsmeront/open-opportunity-platform) | private | Proprietary platform + fulfillment working set (D8 private side) |

The old single public repo was deleted and recreated fresh as `open-opportunity-protocol`
from retained files only — no working-set history (see decisions D8).
